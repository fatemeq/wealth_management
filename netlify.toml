# netlify.toml - NO CHANGES NEEDED (PERFECT)
[build]
  command = "CI=false pnpm install --frozen-lockfile && pnpm build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
